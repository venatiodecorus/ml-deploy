{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "go",
      "label": "go: build package",
      "command": "build",
      "options": {
        "cwd": "${workspaceFolder}/src"
      },
      "args": ["-o", "../deploy"],
      "problemMatcher": ["$go"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "go build"
    }
  ]
}
